# Spec

1. [extra] `openspec/specs/domains.md:5` — adds `resource` under `core` domain catalog; not named in change requirements, `proposal.md` Impact, or `tasks.md`
   → Revert the `domains.md` hunk or record an explicit spec/task requirement if domain registration is intended in-scope for this change.
   Status: skipped
   Argument: not missing/wrong; librarian require domain allowlist when the first `core_resource_*` spec is added.
