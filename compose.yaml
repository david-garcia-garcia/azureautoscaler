services:
  app:
    build:
      context: ./autoscaler
      dockerfile: Dockerfile
      args:
        VERSION: ${VERSION:-0.0.0-dev}
    image: ${IMAGE}
    volumes:
      - /autoscaler/config.json:/app/config.php