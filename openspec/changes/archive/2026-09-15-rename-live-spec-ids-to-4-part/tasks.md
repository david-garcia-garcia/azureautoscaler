# Tasks

## 1. Catalog renames
- [x] 1.1 `git mv` each kebab live folder to its 4-part id per `proposal.md` (eleven moves)
- [x] 1.2 Update `openspec/specs/domains.md` allowlist (`core`: forecast, resources, config, scaling; `std`: docs)
- [x] 1.3 Run `validate_spec_map` write + verify and `validate_artifact_names`

## 2. Cleanup
- [x] 2.1 Delete `knowledge/debt/2026-09-15-rename-live-spec-ids-to-4-part.md`
- [x] 2.2 Grep and fix any live-path references outside archived changes and foreign devstate

## 3. Verify
- [x] 3.1 Run solution tests (`dotnet test`)
